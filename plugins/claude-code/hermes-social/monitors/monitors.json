[
  {
    "name": "hermes-inbox",
    "command": "curl -sN -H \"Authorization: Bearer ${HERMES_SOCIAL_TOKEN}\" \"${HERMES_SOCIAL_URL:-http://localhost:8340}/v1/inbox/stream\"",
    "description": "Streams new inbound A2A messages from hermes-social as SSE events. Requires the /v1/inbox/stream endpoint on the backend."
  }
]
